//To find any string in the field
var value = current.variables.variablename.getDisplayValue();
if(value.indexOf('string') > -1){	
//to find a particular string example: Production
	current.fieldname = value;
  //if the value is found then set the value
	}
