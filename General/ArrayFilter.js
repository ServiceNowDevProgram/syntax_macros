/**
* Description : The .filter() method is used to create a new array from a given array consisting of only those elements from the given array which satisfy a condition set by the argument method.
* @orignalArray : Array which needs to be transformed
* @newArray : Array transformed from orignal array using filter
* @return : Transformed Array element						
*/

var newArray = orignalArray.filter(function(val, index){
	// Business logic to be worked on each element
	// If evaluate to true then pushed to newArray else ignored
	return ;
})
