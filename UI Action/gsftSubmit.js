gsftSubmit(null, g_form.getFormElement(), 'action_name');

if (typeof window == 'undefined')
    serverSideFunction();

/** Server-side function to process the current record and navigate to necessary page**/
function serverSideFunction() {
  // server side script here
}
