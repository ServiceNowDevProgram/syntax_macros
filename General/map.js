/**
* Description : The map() method creates a new array with the results of calling a function for every array element.
* @orignalArray : Array which needs to be transformed
* @newArray : Array transformed from orignal array using MAP
* @return : Transformed Array element						
*/

var newArray = orignalArray.map(function(val, index){
	// Business logic to be worked on each element of orignalArray
	return ;
})
